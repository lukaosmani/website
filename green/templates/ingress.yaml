apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: green-website-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: green.luka.com
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: green-website
                port:
                  number: 80
